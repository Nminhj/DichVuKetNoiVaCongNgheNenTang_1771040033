# Versioning

This repository follows a simple contract versioning approach for Lab 02.

- `1.0.0` — initial negotiated API contract for Camera Stream ? AI Vision.
- Changes to endpoints, request/response formats, or error contract must be followed by a new version and an updated negotiation log.

Current contract version: 1.0.0
